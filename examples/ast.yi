функ старт() {
	змінна а = 5 + 3 * (2 - 8) / -4;
	змінна б = а * 9 + 1;
	змінна в = (б - 2) / (а + 7);
	змінна г = в * (б + а) / 3;
	змінна д = г + в - а * б / 2;
	змінна е = д * -1 / (г + 5);
	змінна ж = е + а - б * в / г;
	змінна з = ж * 2 + е - а / б;
	змінна і = з + ж - д * г / е;
	змінна й = і * 1 / (з + 3);
	змінна к = й + і - з * ж / е;
	змінна лічильник = к * 2 + й - і / з;
	змінна фінальне_значення = (лічильник + к) * (й - і) / (з + ж);

	друклн_і64(фінальне_значення);
}
